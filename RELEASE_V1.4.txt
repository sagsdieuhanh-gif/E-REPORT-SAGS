E-REPORT SAGS V1.4 — 18/08/2026
Build: V1.4-20260818-02

V1.4 BUILD 02 — AI CROSSCHECK STABLE OUTPUT
- Giữ nguyên toàn bộ nghiệp vụ và giao diện V1.4 Build 01; không đưa các thay đổi Firebase Authentication/Security vào bản này.
- Tăng maxOutputTokens AI CROSSCHECK: 1400 → 4096.
- Response schema giới hạn tối đa 15 differences và 6 observations.
- Prompt buộc kết quả ngắn gọn, JSON thuần, giới hạn chiều dài summary/reason.
- Parser JSON chịu được code fence/chữ thừa trước-sau object.
- Kiểm tra finishReason; nhận diện MAX_TOKENS/JSON lỗi/response rỗng.
- Khi output chưa hoàn chỉnh: tự retry đúng 01 lần bằng chế độ rút gọn (10 differences, 4 observations).
- Diagnostic hiển thị rõ AI-OUTPUT-TRUNCATED / AI-JSON-INVALID / AI-EMPTY-RESPONSE và lưu finishReason/token metadata trong Chi tiết kỹ thuật.
- HDSD đã bổ sung cách AI tự retry và cách AD dùng CHẨN ĐOÁN AI khi vẫn lỗi.
- Đồng bộ version.json, APP_BUILD_VERSION và Service Worker cache sang V1.4-20260818-02.

V1.4 BUILD 01
- Sửa Firebase Web API key bị sai 1 ký tự hoa/thường trong V1.3.
- AI CROSSCHECK: thêm CHẨN ĐOÁN AI (CONFIG → APP CHECK → FIREBASE AI LOGIC → MODEL).
- Khi AI lỗi: hiển thị mã lỗi, vị trí lỗi, việc cần làm và chi tiết kỹ thuật.
- GIAO CA: hiển thị chính xác lỗi Firestore.
- GỬI FINAL: không còn nuốt lỗi RTDB do Promise.allSettled; nếu Firestore lưu được nhưng RTDB FINAL/CROSS_STATE thất bại sẽ cảnh báo ngay.
- Frontend tiếp tục chỉ host trên GitHub Pages; Firebase chỉ là backend.

E-REPORT SAGS V1.5 — 18/08/2026
Build: V1.5-20260818-01

V1.5 — AI CROSSCHECK NHANH HƠN + R&S DELIVERY RECOVERY
- Kế thừa V1.4 Build 02; không đưa thay đổi Firebase Authentication/Security vào bản này.
- Giữ model Gemini 3.6 Flash, tối ưu payload ảnh: FINAL tối đa 1400 px; ảnh CHECK tối đa 1500 px; ưu tiên JPEG gọn để giảm dung lượng gửi AI.
- maxOutputTokens: 3072; tối đa 12 differences + 5 observations. Retry lỗi output vẫn tối đa 01 lần, retry rút gọn 8 differences + 3 observations.
- Khi AI chạy, panel hiển thị công đoạn + số giây. Kết quả lưu timing init/images/generate/total và hiển thị thời gian tổng khi hoàn tất.
- Sửa lỗi READ & SIGN: V4.70 dùng listener người nhận RTDB-only trong khi SEND bỏ qua lỗi RTDB bằng Promise.allSettled, nên có thể AD báo gửi thành công nhưng người nhận không thấy.
- V1.5 người nhận nghe đồng thời RTDB realtime + Firestore mailbox fallback và hợp nhất hai nguồn. R&S cũ đã SEND nhưng RTDB không tới có thể tự phục hồi từ Firestore mailbox khi người nhận chạy V1.5.
- SEND R&S kiểm tra kết quả RTDB, tự retry 01 lần người bị lỗi và hiển thị RTDB x/y; Firestore mailbox vẫn là đường dự phòng.
- Đồng bộ HDSD, version.json, APP_BUILD_VERSION, cache-bust AI/Firebase config và Service Worker cache sang V1.5-20260818-01.

LƯU Ý TEST
1. Cập nhật cả máy AD và máy người nhận lên V1.5.
2. Đăng nhập máy người nhận trước; để app mở.
3. AD SEND một R&S test tới đúng vai trò/đơn vị.
4. AD phải thấy trạng thái RTDB x/y. Nếu RTDB thấp hơn tổng, máy nhận vẫn phải thấy R&S qua Firestore fallback.
5. Kiểm tra R&S cũ đã gửi trước V1.5: sau khi người nhận cập nhật/đăng nhập, mailbox Firestore sẽ được nghe trực tiếp và danh sách có thể phục hồi.
6. AI: thử 1 case MATCH và 1 case có nhiều sai lệch; theo dõi số giây trên panel và thời gian tổng sau khi hoàn tất.

E-REPORT/SAGS V3.7 – AUDIT / IMPLEMENTATION STATUS
Date: 21/08/2026
Build: V3.7-20260821-01
Basis: Yêu cầu kiểm soát giai đoạn Pilot + Bảng khung yêu cầu tổng thể E-OPS.

A. ĐÃ THỰC HIỆN TRONG V3.7
1) 5 cửa GO/NO-GO trước Pilot thật:
   - SOP chạy song song.
   - Critical Data + Data Owner.
   - Permission + Cross-check test.
   - Fallback Dry Run.
   - Đào tạo nhân sự Pilot.
   Thiếu 1 cửa -> NO-GO; không bật STRICT PILOT MODE.

2) Critical Data / Data Owner / 4 quyền:
   - Ma trận VIEW / INPUT / VERIFY / APPROVE riêng.
   - SYSTEM ADMIN không mặc nhiên là DATA OWNER/APPROVER.
   - Không tự suy diễn các Owner/Approve tài liệu chưa chốt: giữ CHUA_CHOT/THEO_SOP.
   - Khi STRICT mode: đã nối kiểm soát trước vào GỬI KẾT SỔ (PAX FINAL), GỬI FINAL (LOAD CONTROL) và CROSSCHECK OK.

3) Control Audit append-only:
   - RTDB path: sags_live_v470/pilot_control_v37/audit.
   - Ghi actor/principal, flight context, event, reason, before/after/detail.
   - System Timestamp dùng Firebase RTDB ServerValue.TIMESTAMP.
   - Không có UI sửa/xóa audit.

4) Operational Time:
   - Phân biệt Control Audit System Timestamp với giá trị Operational Time người dùng nhập.
   - Khi STRICT mode và sửa một Operational Time đã có, bắt buộc nhập lý do; không có lý do thì revert về giá trị cũ.
   - Lần nhập đầu tiên được audit nhưng không bắt lý do điều chỉnh.

5) BCP/Fallback:
   - Cấu hình phạm vi Pilot, người/cấp quyết định fallback, đối tượng thông báo, điều kiện quay lại.
   - State: NORMAL / DEGRADED / FALLBACK / RECOVERY.
   - Mọi transition bắt buộc lý do + audit.
   - Khi state != NORMAL, tất cả client nhận banner trạng thái realtime.
   - Trạng thái phần mềm không tự quyết định dừng hoạt động khai thác.

6) Dry Run:
   - Ghi nhận kịch bản mất mạng, server/app lỗi, device, account, data access, AI/OCR sai, mismatch critical data.
   - PASS/FAIL + ghi chú + người thực hiện + timestamp.

7) Issue Log / CAPA:
   - Flight, module, error class, severity, impact, workaround, root cause, CAPA, status.
   - Phân loại System/User/Data/Process/Network-Device/False Warning/Safety-Near-miss.
   - Đổi status issue bắt buộc lý do + audit.

8) Security-ready identity abstraction:
   - Control layer ưu tiên uid/firebaseUid/authUid nếu tài khoản đã có.
   - Nếu chưa có, đánh dấu LEGACY_PRE_AUTH và dùng LEGACY:<username> làm principal tạm.
   - Chuẩn bị để migration sang Firebase Auth không phải viết lại Control Audit/Pilot module.

B. KIỂM TRA KỸ THUẬT
- 27 standalone JavaScript: syntax PASS.
- 25 inline script trong index.html: syntax PASS.
- Service Worker precache: 72/72 file tồn tại.
- APP_BUILD_VERSION: V3.7-20260821-01.
- APP_DISPLAY_VERSION: V3.7 AI.
- version.json / Service Worker cache đã đổi sang V3.7.
- HDSD V3.7 đã bổ sung.

C. CHƯA ĐƯỢC COI LÀ HOÀN THÀNH / CẦN LÀM TIẾP
P0 SECURITY – BẮT BUỘC TRƯỚC PILOT CHUYẾN THẬT:
- Chưa cutover sang Firebase Authentication chính thức.
- RTDB rules hiện vẫn mở read/write cho sags_live_v470; Firestore sags_handovers vẫn allow true.
- Chưa có server-side authorization thực sự cho VIEW/INPUT/VERIFY/APPROVE.
- Chưa có MFA cho AD/Approver.
- Chưa harden session timeout / revocation theo Firebase Auth.
=> V3.7 Control Layer là PRE-AUTH, KHÔNG được coi là Security-complete.

P0/P1 CONTROL CÒN PHẢI MỞ RỘNG:
- Các Critical Data ngoài PAX FINAL / LOAD FINAL / CROSSCHECK chưa nối toàn bộ action vào Permission Service.
- Data Owner/Approve của BAG Final, LIMIT, Damage... phải được nghiệp vụ chốt theo SOP trước khi dùng STRICT.
- Rules Engine hiện hữu chưa được migrate toàn bộ sang cấu hình trung tâm; V3.7 mới seed threshold config để chuẩn bị.
- Chưa có AUTHORIZED OVERRIDE engine đầy đủ cho tất cả điểm khóa.
- Dashboard Green/Yellow/Red, countdown, KPI Pilot, alert-quality metrics chưa hoàn thiện thành dashboard thống nhất.
- File/evidence storage governance + checksum/retention/server-side access vẫn cần hoàn thiện.

D. KHUYẾN NGHỊ TRIỂN KHAI
- Dùng V3.7 để DRY RUN và chốt Data Owner/Approve theo SOP.
- Không bật STRICT PILOT MODE cho chuyến thật nếu 5 gate chưa đạt.
- Sau khi control workflow ổn: thực hiện Security Cutover (Firebase Auth + Rules + MFA + session) trước Pilot live.
